# API versioning

HTTP endpoints are prefixed with `/api/v1`. New incompatible contracts should use `/api/v2`; additive fields remain compatible within the current version. Swagger documents the active versioned contract.
